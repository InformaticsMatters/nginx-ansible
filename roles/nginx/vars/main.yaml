---

# The NGINX version.
nx_image_tag: '1.31.2-alpine'

# CPU and Memory requests and limits (very low)
nx_cpu_request: 100m
nx_cpu_limit: 100m
nx_mem_request: 32Mi
nx_mem_limit: 32Mi

nx_cert_issuer: production

# General variables

wait_timeout: 120
